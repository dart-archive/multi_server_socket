name: multi_server_socket
version: 1.0.3-dev

description: A dart:io ServerSocket wrapper that listens on multiple ports.
homepage: https://github.com/dart-lang/multi_server_socket

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  async: '>=1.2.0 <3.0.0'

dev_dependencies:
  test: ^1.3.0
  pedantic: ^1.0.0
